                        <span class="onnet-07em" style="vertical-align:bottom;">If falls below
                        {{ m.session.currency_sign }}</span>
                        <input class="input-card-field" type="text" name="threshold" maxlength="3" size="3" value="{{ topup[1]["threshold"] }}" />
                          / 
                        <span class="onnet-07em" style="vertical-align:bottom;">Recharge with
                        {{ m.session.currency_sign }}</span>
                        <input class="input-card-field" type="text" name="amount" maxlength="3" size="3" value="{{ topup[1]["amount"] }}" />

